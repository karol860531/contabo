pipeline {
    agent any

    stages {
        stage('Verify_branch_name') {
            steps {
                echo "$GIT_BRANCH"
            }
        }
            stage('pwd') {
            steps {
                sh 'pwd'
            }
        }
    }
}
