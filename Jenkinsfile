pipeline {
    agent any

    stages {
        stage('Verify') {
            steps {
                echo '$GIT_BRANCH'
            }
        }
            stage('pwd') {
            steps {
                sh 'pwd'
            }
        }
    }
}
